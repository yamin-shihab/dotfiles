abbr -a cp cp -iv
abbr -a df df -h
abbr -a du du -abhx -d 1
abbr -a free free -h
abbr -a grep grep -n
abbr -a ip ip --color=auto
abbr -a ls ls -hvAG --group-directories-first
abbr -a mkdir mkdir -pv
abbr -a mv mv -iv
abbr -a rm rm -iv
abbr -a sort sort -h
