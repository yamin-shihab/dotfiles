#!/usr/bin/env sh

xrdb "$HOME/.config/xresources/xresources"

for f in /etc/X11/xinit/xinitrc.d/?*.sh; do
    source "$f"
done

export XDG_CURRENT_DSEKTOP="qtile"
export XDG_SESSION_DESKTOP="qtile"

openrgb -p default &
urxvtd &
exec qtile start
